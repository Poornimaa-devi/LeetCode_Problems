        int currmax=nums[0];        for(int i=0;i<n;i++){            currmax=Math.max(currmax,nums[i]);            if(currmax-suffixmin[i]<=k) return i;        }            suffixmin[i]=Math.min(suffixmin[i+1],nums[i]);        for(int i=n-2;i>=0;i--){        }        suffixmin[n-1]=nums[n-1];        int suffixmin[] = new int[n];        int n = nums.length;    public int firstStableIndex(int[] nums, int k) {